import { ProductImagesDirective } from './product-images.directive';

describe('ProductImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductImagesDirective();
    expect(directive).toBeTruthy();
  });
});
